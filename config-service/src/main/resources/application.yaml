server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: multiple-instance
          uri: https://github.com/yhordan/Clase-Microservicio.git
          search-paths: config-data
  application:
    name: config-service
